# Review of lane A branch (head 527ac353)

*Independent review, 2026-09-08, by the coordinating reviewer (Claude Fable). Full build performed. Items 1–5 are required before the lane is considered landed; 6–10 are recorded open work.*

## Independent review of this branch (head 527ac353, 52 commits on 9a951436)

**Verdict: not mergeable as-is; mergeable into the integration branch after items 1–5 below (mechanical), with items 6–10 as the recorded open work.** The bytes are sound: `lake build Lib` and the full consumer chain (`Solution`, `S6`, `S6Shortcuts`, `Challenge`) are green with zero errors; sixteen top theorems across lanes A, D1, D2, H, I, B print exactly `[propext, Classical.choice, Quot.sound]`; every one of the 16,368 declarations left in `Hopf/` is byte-identical to its base text; the fifteen moved files I sampled reproduce the 721fc82 bytes line for line after normalizing header, imports, namespace lines and the announced rename map; the stock count fell 9,408 → 5,170 by moves alone. What is not yet true is most of the extraction-mode deliverable around the bytes: provenance receipts, module docstrings, section headers, per-declaration docstrings, `Lib.lean` registration, and the FREE-vocabulary rule inside `Lib/`. "Not perfect but a great start" is accurate for the byte-moving; the textbook-recovery half of extraction mode has essentially not started outside lane A's sixteen module docstrings.

### Checks

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | Build | PASS | `lake build Lib`: 143 s, 8,774 jobs, 0 errors, 78 warnings. Consumers (`Solution S6Shortcuts S6 Challenge`; there is no `Hopf.lean` root): 896 s, 8,812 jobs, 0 errors, 79 warnings (the same 78 + one pre-existing `sorry` in the unchanged `Challenge.lean`). All 78 are `linter.dupNamespace` in `Lib/Topology/Homotopy/Suspension.lean` (64) and `Lib/Topology/Homotopy/LocalCollapse.lean` (14). |
| 2 | Placeholders | PASS | No `sorry`/`admit`/`axiom`/`native_decide` in `Lib/` or `Hopf/` code; no `maxHeartbeats` added; no `PLACEHOLDER`/SPDX in new files. |
| 3 | Axioms | PASS | 16/16 probes exact (A: `SingularMayerVietoris.exact_at_ambient`, `SphereHomology.unitSphere_homology_subsingleton`, `SphereHomology.suspensionHomologyHigherEquiv`, `SingularHomology.homotopic_homologyMap`; D1: `Smale.ManifoldMorse.exists_morse_function`, `SmoothMorseLemma.exists_signed_morse_chart_of_contDiffOn`, `…exists_attachingUnionHomeomorph_with_level_and_orbits`; D2: `Smale.exists_tubularNeighborhood_in_open_of_embedded_closedBall`, `Degree.MorseCells.exists_disjoint_cells`, `Smale.NativeTransversality.exists_ambient_transverse_diffeomorph`; H: `RiemannMapping.exists_bijOn_unitBall_deriv_ne_zero_map_eq_zero`, `HolomorphicCousin.exists_normalized_holomorphic_cocycle_solution`, `AnalyticRootCover.exists_analytic_square_root`; I: `MappingTorusHomology.twoArc_exact_at_pair`, `SplitGroupExtension.mulEquiv`; B: `FundamentalGroupVanKampen.TwoOpenCover.pushoutEquiv`). The build also prints `mathoverflow_1973` with the same three. |
| 4 | Verbatim claim | PARTIAL | Content matches in all 15 sampled files (A ×6, D1 ×5, H ×2, I ×2). Receipts do not: only 7 of 34 baseline commits contain a SHA-256; of five testable, one reproduces from 721fc82 (`bd7a4b5`). `90685ed` says its hash was taken "at current HEAD"; `720a5ae`'s hashed range includes a declaration that stayed behind; `e997d6c`'s two hashes match neither range. Five stated ranges start one line after the declaration head (1863→1861, 3660→3658, 4017→4016, 331→330, 870→869) and Cousin's ends 7 lines short. `Sum.lean`'s stated range covers 106 of its 317 lines. The eleven lane-A files (Suspension ×2, Sphere, SphereHomology, Sum, CircleProduct, Coproduct, LocalContributions, Naturality, LocalDegree, OnePointCollapse) arrived silently in `44c11efc` ("lib(D1): chart transport and cylinder HEP") after a history repair; their baseline messages and the rename commit the report cites (`1e8c699`) are not on the branch. |
| 5 | Statement-change rule | PASS | 0 new and 0 changed declarations in `Hopf/` (per-declaration comparison against base). Non-import additions: `Hopf/LibShims.lean`, the import lines, and three identical `attribute [local instance] Smale.NativeEuclideanEmbedding.tangentSpaceT2` lines at `Hopf/DifferentialTopology.lean:98,100,102`. |
| 6 | FREE rule | PARTIAL | Transitional namespaces (`Mathoverflow1973`, `Smale.*`, `NoExotic.*`, `Degree.*`, `MorseCancel.*`, `Smale.Native*`) are allowed by Q2. Violations (21 lines): a project-namespace declaration `SpecialPeriods.exists_analytic_unit_root` (`Lib/Analysis/Complex/SquareRoot.lean:60`); 20 module-docstring lines citing pre-rename names that no longer exist in `Lib/` (`Sum.lean:17,40,41`; `CircleProduct.lean:22,25,45,46`; `HomotopyInvariance.lean:18,24,39,45,46,48`; `SingularHomology/Suspension.lean:26,49`; `Topology/Homotopy/Suspension.lean:16,35,36`; `OnePointCollapse.lean:16`; `Naturality.lean:22`); one `Hopf.Recognition` mention at `Lib/AxiomAudit.lean:68`. |
| 7 | Docstrings | FAIL | 63 new `Lib/` files: 24 have no module docstring at all (all of lanes B and I, `CrossInsert`, `PathClass`, and D1's `Index`, `SublevelSets`, `ChartedSpace/Transport`, `CylinderHEP`); 17 have the full reference shape; 20 lack `## Outline`; `SurgeryWindows` lacks main results/references/tags. `/-! ### -/` section headers: 62 of 63 files have none. Public declarations without a docstring: 4,245 of 4,246. |
| 8 | Stock rule | PASS | 9,408 → 5,170; `--check` passes; the base 57-entry prefix list applied to the branch also gives 5,170 (32 prefixes now absent), so the drop is from moves, not list edits. The four removed entries have zero declarations left in their scope. |
| 9 | Shims | PASS | One file, `Hopf/LibShims.lean`: 15 `export` lines, 541 unique names (report says 449), 8 target namespaces, imported by all 16 `Hopf` modules; all resolve. |
| 10 | Report accuracy | FAIL | (1) The shared Lean copy the report calls stale ("Mathlib c5ea003, v4.30-era") is at Mathlib `db584cd6d4` with a v4.33.0 toolchain. (2) "SHA-256 of every source range recorded" — 7 of 34 commits, none in the report, though `d3386bb4` says they are preserved there. (3) 14 lane-A commit hashes in the table do not exist on the branch. (4) "module docstrings for the eleven D1 files" — 8 files; four D1 files have none. (5) "Lane B movable material: COMPLETE" — the plan's B probe `simplyConnectedSpace_of_open_cover` is still `Hopf/Hurewicz.lean:550`. (6) "Lib.lean root completed (66 modules)" — 10 later files never registered. (7) "449 names" — 541. (8) SurgeryWindows described as D2's range DT 10392–14114 — by 721fc82 position the file is E2 391, E1 278, F 70, D1 59, unlisted 56, D2 28 declarations. (9) The E1 drafts are said to be preserved in a notes directory — they are not on the fork; only the author's machine has them. Verified true: all axiom receipts, green builds, census 5,170, `git diff --check` clean, SurgeryWindows size, the comparator blocker. |
| 11 | Scope | FAIL (process) | Seven lanes on one branch instead of one per lane; one report for all lanes; a history repair that folded lane A into a D1 commit; `753ecf86` moved 461 declarations from lanes E2/F (Kimi's) and 278 from E1 into one 17,556-line file under a D2 label. Consequence: no per-lane review or revert; lane A's rename is inseparable; E2/F must start from a file they did not choose. Out-of-scope files were not touched. Not a mathematics defect. |
| 12 | Kimi hand-off | PARTIAL | See below. |

### Actionable items (by severity)

1. **Provenance receipts.** Add to the report a table `721fc82 file:range → SHA-256 of that exact range → target file` for every baseline, covering the 27 commits without one and the eleven lane-A files inside `44c11efc`; recompute the four non-reproducing hashes with ranges cut at the declaration boundary. History need not be rewritten; the report must carry the receipts.
2. **Register the ten unrooted modules in `Lib.lean`:** `AlgebraicTopology.FundamentalGroup.{SimplyConnectedCover, TwoSimplyConnectedCover, VanKampen}`, `AlgebraicTopology.FundamentalGroupoid.SimplyConnectedSphere`, `Geometry.Manifold.Morse.SurgeryWindows`, `Geometry.Manifold.Quotient.{Atlas, LocalOrbit}`, `Topology.Algebra.FreeActionLocus`, `Topology.Homotopy.LoopSubdivision`, `Topology.MappingTorus.Basic`. They build today only because `Hopf/` consumers import them.
3. **FREE rule inside `Lib/`.** Rename `SpecialPeriods.exists_analytic_unit_root` (`SquareRoot.lean:60`, `:28`, `:284`) with a shim; replace the 20 stale pre-rename names in the docstrings listed under check 6; drop the `Hopf.Recognition` comment in `Lib/AxiomAudit.lean:68`.
4. **Fix the 78 `dupNamespace` warnings** (`Suspension.Suspension` at `Lib/Topology/Homotopy/Suspension.lean:86,93,…`; `LocalCollapse.LocalCollapse` at `Lib/Topology/Homotopy/LocalCollapse.lean:54`): one rename commit, no proof term changes, shim updated. Mathlib CI rejects lint warnings.
5. **Correct the report** (check 10, items 1–9) and split it into `Lib/reports/{A,D1,D2,H,I,B,E1}.md` as the task requires. In particular: lane B is not complete while `simplyConnectedSpace_of_open_cover` is in `Hopf/Hurewicz.lean:550`; state the true docstring counts (24 files without a module docstring, 4,245 declarations without one); state SurgeryWindows' per-lane composition.
6. **SurgeryWindows — owner decision.** `Lib/Geometry/Manifold/Morse/SurgeryWindows.lean` is 44 % lane E2, 31 % E1, 8 % F by source position; its name and docstring describe the 28 D2 declarations. Either split along the plan's E1/E2/F targets (a pure move of moves) or record the composition and hand the file to the E2/F owner as their starting point. Either way add `## Main definitions and results`, `## References`, `## Tags`.
7. **Module docstrings** for the 24 files without one; `## Outline of the proof` for the 20 that lack it; `/-! ### -/` section headers in proof order for the 62 files that have none.
8. **Per-declaration docstrings:** 4,245 of 4,246 new public declarations. The largest open item; the report should carry this number.
9. **Twin files and rename commits.** No target names a Mathlib twin file (textbook theorems are cited instead); rename commits exist only for lane A and `CuspRetraction.Patching → LocalCollapse`. `Smale.*`, `NoExotic.*`, `Degree.*`, `MorseCancel.*`, `SingularMayerVietoris.*`, `SphereHomology.*`, `RiemannMapping.*`, `HolomorphicCousin.*`, `MappingTorus*`, `FundamentalGroupVanKampen.*` and `namespace Mathoverflow1973` are still transitional. Per the README a lane is not done until its rename has landed.
10. **Hygiene.** Deduplicate the three identical `attribute [local instance] … tangentSpaceT2` lines (`Hopf/DifferentialTopology.lean:98,100,102`); 46 classic vs 17 module-form headers and 61 wholesale `import Mathlib` are recorded deviations a Mathlib reviewer will ask about; the comparator needs `landrun` (absent on the review box as well).

### Hand-off to lanes C, E2, F, G, J

Importable now (all under `namespace Mathoverflow1973`): lane A — `Lib.AlgebraicTopology.SingularHomology.{Chains, ModuleHomology, MayerVietoris, HomotopyInvariance, Sum, Coproduct, CircleProduct, Suspension, Sphere, SphereHomology, Naturality, LocalContributions, LocalDegree, PathClass, CrossInsert}`, `Lib.Topology.Homotopy.Suspension`, `Lib.Topology.OnePointCollapse`, `Lib.Algebra.Homology.MayerVietorisShortExact` (prefixes `SingularChains`, `SingularHomology`, `Suspension`, `Coproduct`, `OnePointCollapse`; `SingularMayerVietoris`, `SphereHomology` unchanged); lane D1 — `Lib.Analysis.Calculus.MorseLemma`, `Lib.Analysis.ODE.SmoothFlow`, `Lib.Geometry.Manifold.{RegularLevel, ChartedSpace.Transport, Flow.Compact, Flow.HeightTranslating, Morse.Handle, Morse.HandleAttachment, Morse.Existence, Morse.SublevelSets, Morse.Index}`, `Lib.Topology.Homotopy.{CylinderHEP, HandleRetraction}`; lane D2 — `Lib.Geometry.Manifold.{WhitneyEmbedding, VectorBundle.ProjectionBundle, Collar, Morse.CellStructure, Morse.SurgeryWindows}`, `Lib.Topology.Homotopy.CellAttachment`; lane B — `Lib.AlgebraicTopology.FundamentalGroup.{VanKampen, SimplyConnectedCover, TwoSimplyConnectedCover}`, `Lib.AlgebraicTopology.FundamentalGroupoid.SimplyConnectedSphere`. Import the unrooted ones by module name until item 2 lands.

Still missing for C: the augmentation block was not moved (`SecondHurewicz.SimplyConnected.chainAugmentation*` at `Hopf/Hurewicz.lean:6547–6568`, `HigherHurewicz.chainAugmentation_boundary` at `:15202`); the cross-product cluster (~106 declarations) stays in `Hopf/Hurewicz.lean`; `Smale.LinearSphereAction.*` stays in `Hopf/SphereTopology.lean`. The SurgeryWindows web is landed and confirmed built (`Smale.Hemisphere.*` inside it). E1: no E1 file landed; 278 E1 declarations are already inside `SurgeryWindows.lean`; the three E1 probe theorems remain in `Hopf/` (`SingularHomology.lean:10501`, `SphereTopology.lean:3508`, `SphereTopology.lean:8635`); the E1 drafts are not on the fork — please commit them (a `drafts/` branch or `Lib/reports/`) before E1 resumes. E2/F: 391 E2 declarations (including the E2 probe `Smale.NativeTransversality.exists_ambient_transverse_diffeomorph`, `SurgeryWindows.lean:17495`, axioms exact) and 70 F declarations are already in `Lib/` inside `SurgeryWindows.lean`; E2's generalization is now an in-`Lib` refactor of that file. J: untouched, as required; the Wang sequence (I) is blocked on the cross product.

### Hashes

Base `9a951436`; head `527ac35342ced246370c408c4cfdc040a210e7cc`; ranges refer to `721fc82ddbb6cb4d2221bacec0c030636a276f3e`. Diff 85 files, +59,230 / −55,031; `Hopf/` −55,026; 63 new `Lib/` files; `git diff --check` clean. Stock 9,408 → 5,170. Builds: Lib 143 s / 0 errors / 78 warnings; consumers 896 s / 0 errors / 79 warnings.
