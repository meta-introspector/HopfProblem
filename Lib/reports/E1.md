/-! Review-A item 5: this file was split out of the monolithic lane report on
branch `lib/A-surgerywindows-split` (off bda000e). Corrections from review A check 10 are
applied inline (marked **[corrected]**). Provenance receipts for all lanes: `Lib/reports/RECEIPTS.md`.
-/

# Lane E1 report — Morse theory II (rearrangement, cancellation, births)

**Current status (2026-09-13, GLM seat run by Devin/Astra):** the current-tree
probe-closure extraction is landed in `Lib/` with exact standard axioms. The
historical attempts below are retained as history and superseded by the final
receipt at the end. Library layout, per-declaration documentation and upstream
API polish remain separate follow-ups; they are not additional Hopf moves in
this probe-closure boundary.

E1 is the largest lane: ~700 declarations over 22 families. Historical
dependency findings from the DAG closure scans:

- `Degree.FlowTimeChange` (55) is mid-stack: its closure needs
  FlowSuspension native cylinders, FlowCancellation levelBasin,
  LocalFunctionReplacement, and MorseCancel band normalization (23 refs).
- `Degree.FlowSuspension` (91) is also mid-stack: 29 Hopf refs incl.
  MorseCancel.Model and the cubic-cylinder cluster.
- The true base layer is the **cubic model cluster**: MorseCancel.Model +
  cubic descent/flow-cylinder + AxisCoordinates + Regular/Signed height
  coordinates + Smale.PartialChart — closure of 197 declarations.

**Proposed E1 unit order (resume here):**
1. `Morse/Cubic.lean` (+`CubicFlow.lean`) — the 197-decl cluster (one
   baseline commit; the plan's finer file split should follow the
   declaration cut, not precede it)
2. `Flow/LevelCylinder.lean` — FlowSuspension (91) [draft exists]
3. `Morse/Cancellation-support` — FlowCancellation (71) +
   LocalFunctionReplacement (14)
4. `Flow/TimeChange.lean` — FlowTimeChange (55) [draft exists]
5. `Flow/PhaseChart.lean` — TransverseGerms (32); `FieldChartGluing.lean` (13)
6. `Morse/Rearrangement.lean` — MorseRearrangement (74) +
   SupportedDiffeomorph (19) + PartialChart (2)
7. `Morse/Cancellation.lean` — MorseCancel remainder (~124) + AdaptedWindows (13)
8. Splits for IndexOrdering / SuperfluousMinima / Birth / Duality follow the
   landed declaration cut.

Drafts (red, unlanded) are preserved as `Lib/reports/drafts/Cubic.lean.draft`,
`Lib/reports/drafts/TimeChange.lean.draft`, and
`Lib/reports/drafts/LevelCylinder.lean.draft`.
Axiom probes pending: exists_morse_rearrangement_of_no_connection,
cancel_of_transverse_level_isotopy, exists_excellent_indexed_morse_birth.

---

# Lane E1 unit-1 attempt (session 3 close): BLOCKED at the SurgeryWindows web

The 197-decl cubic closure grew to 291 under definition-closure (the full
MorseCancel/AdaptedWindows/AxisCoordinates/PartialChart neighborhood) and
still fails: the kept material references `AdaptedWindows` (the band
Lyapunov structure), whose family is interleaved with the MorseSurgeryData /
SurgeryWindows web — the ownerless obstruction recorded at the D2 stage.

Consequence: lane E1's base layer is coupled to the SurgeryWindows decision
(the same one gating lane C's augmentation and lane G). Until that web gets
its Stage-4 treatment (owner decision: rename-and-move as generic Morse
window data, or keep as project code with an interface seam), the movable
E1 units are the flow-side leaves only, and each of those needs the cubic
core. E1 therefore joins C/F/G in the blocked column.

Drafts preserved: drafts/Cubic.lean.draft (291-decl extraction),
drafts/TimeChange.lean.draft (55), drafts/LevelCylinder.lean.draft (91),
plus cfg-tc/cfg-lcyl/cfg-cubic and the definition-closure script inline in
the session history. Resume order in the previous section stands, with the
amendment: unit 1 requires the SurgeryWindows decision first.

---


---

# Owner decisions applied (session 4) and corrected E1 sequencing

## Decisions (Fabian, session 4)

1. **SurgeryWindows web = lane D2, decision (a), pure move** — it was never
   unowned: DT L10392–14114 is D2's range ("SurgeryWindows/AdaptedWindows
   structures" → `Lib/Geometry/Manifold/Morse/SurgeryWindows.lean`), census
   GENERIC-TEXTBOOK. **Landed: 753ecf8 (879 declarations, 17,492 lines off
   DifferentialTopology).** Citations for the coming docstrings: Milnor,
   *Lectures on the h-cobordism theorem* §3–4 and *Morse Theory* §3 (NOT
   Hatcher §4.2, which is homotopy groups/Whitehead).
2. **Triangle-action tail stays in `Hopf/`** (SpecialPeriods is project); the
   "properly discontinuous ℤ²-action" abstraction is generalize-then-move
   work with an Axis-1 textbook first — named follow-up, not pure move.
3. **Lane G = Kimi's.** Hemisphere declarations that are generic moved with D
   (done: `Smale.Hemisphere.*` in SurgeryWindows.lean); G-specific ones stay.
4. **Q6 resolved by pin-check:** the two duplicated steps ARE in Mathlib at
   db584cd but the module is built private downstream — delete-and-re-route
   is impossible, so the copies stay and the PR draft is recorded above.

## Corrected E1 sequencing

With the web landed, the cubic base (`MorseCancel.Model` family) now lives in
`Lib/Geometry/Manifold/Morse/SurgeryWindows.lean`. The remaining E1 material
is the ST-side `MorseCancel.*` consumers (287 seeds), whose definition closure
over the current tree reaches **1807 declarations** (SH 1057, ST 748) — the
ST/SH files are byte-slices of one solution and cross-reference densely.
**E1 therefore sequences AFTER lane B's sphere/SH remainder**, not before it.
Revised resume order: B remainder (EuclideanSphere 14 → instance-spheres
file, SH 18734–19048 tail) → E1 Cancellation/Rearrangement/Birth baselines
against the then-smaller closure → the flow-side files (TimeChange,
LevelCylinder drafts already exist).

## Campaign state after these landings

Census 9408 → **5192**. Lane B: SimplyConnectedCover (da29618),
TwoSimplyConnectedCover (0b5d47f), VanKampen (eca3d71) landed — Hatcher
Thm 1.20's machinery is now in `Lib`.


---

# Item 7 close (2026-09-12): drafts committed, resume scoped by measurement

The Cubic/TimeChange/LevelCylinder drafts are committed under
`Lib/reports/drafts/` (fe73c3d). The three probe theorems were closure-measured
by attempted extraction (reverted; tree green at fe73c3d):

| probe | Hopf location | dependency closure |
|---|---|---|
| `MorseCancel.cancel_of_transverse_level_isotopy` | SingularHomology 10501–10611 | 16 closure rounds, ~190 declarations — the whole cancellation web incl. `NativeConnectionCancellationData`, `Smale.NativeTransversality.At`, phase-chart and suspension-field material |
| `Degree.MorseRearrangement.exists_morse_rearrangement_of_no_connection` | SphereTopology 3508–3584 | 6 rounds, ~44 declarations |
| `MorseCancel.exists_excellent_indexed_morse_birth` | SphereTopology 8635–8710 | 7 rounds, ~26 declarations |

Conclusion: the three probes are not independent moves; they are the *tips*
of the E1 lane's dependency cone (the plan's ≈20,000-line pure move). The
correct unit of work is the lane-E1 landing itself, done family-by-family
into `Morse/Cancellation.lean` (whose 301 declarations already landed via the
item-0 split), `Morse/Rearrangement.lean`, `Morse/Cubic*.lean`,
`Flow/TimeChange.lean` and `Flow/LevelCylinder.lean`, starting from the
families named above (`NativeConnectionCancellationData` cluster first: it is
the deepest shared dependency of all three probes). Nothing is COMPLETE while
a probe remains under `Hopf/` — recorded, not claimed.

## Current-tree restart (2026-09-13, GLM seat run by Devin/Astra)

The owner superseded the historical 197-declaration cut: the cubic core is
already in `Morse/Cancellation.lean` and is not re-extracted. The committed
drafts are evidence only. E1 now follows the dependency closure inside `Hopf/`
of `MorseCancellation.cancel_of_transverse_level_isotopy`,
`MorseRearrangement.exists_morse_rearrangement_of_no_connection`, and
`MorseCancellation.exists_excellent_indexed_morse_birth`, beginning with
`NativeConnectionCancellationData`. Splitting `Cancellation.lean` into cubic
files is a later layout change.

The first baseline moves the two data structures `NativeEndpointSliceData`
and `NativeConnectionCancellationData` from `Hopf/SingularHomology.lean` to
`Lib/Geometry/Manifold/Morse/Cancellation.lean`, in that dependency order.
They record cubic endpoint slices, the intervening flow cylinder, and a unique
connecting orbit. The cancellation argument consuming these data follows
Milnor, *Lectures on the h-cobordism theorem*, Theorem 5.4; these structures
are its coordinate bookkeeping, not the cancellation theorem itself.
The existing module overview remains the textbook entry point; detailed data
structure docstrings are a later documentation-only change.

The `Transverse` predicate and `cancel` theorem remain in `Hopf/` for the next
unit. `Transversality/Basic.lean` already imports `Cancellation.lean`, so the
predicate cannot be moved into `Cancellation.lean` by adding the reverse
import. All three lane probes remain in `Hopf/`; E1 is not complete.

### Unit 1 provenance and verification

Source revision: `6b18ade` (Lean sources identical to upstream `37fc1de8`).
Ranges include the final newline; hashes are SHA-256 of UTF-8 source bytes.

| Source in `Hopf/SingularHomology.lean` | Declaration | SHA-256 |
|---|---|---|
| 4247–4299 | `MorseCancellation.NativeEndpointSliceData` | `863e4547e29f63eff37bf68a07ec9dee6763d245f403c98032be5c331e3eb5b3` |
| 9767–9812 | `MorseCancellation.NativeConnectionCancellationData` | `97825f5e1da7a709dd063018b2ffaae7d8a8eb63bd5631cc899e8b1e9319b73e` |

Both blocks occur byte-for-byte exactly once in `Cancellation.lean` and no
longer occur as declarations in `Hopf/SingularHomology.lean`. No proof term
or statement changed. The only import edit is the explicit
`Lib.Geometry.Manifold.Morse.Cancellation` import in the source consumer.
The target was already registered in `Lib.lean`; no shim or rename was needed.
Stock census: **2,663 → 2,661**, baseline lowered with
`python3 scripts/lib_stock_census.py --update`.

Verification with pinned Lean 4.33.0:
- `lake build Hopf.SingularHomology`: exit 0.
- `lake build Lib Solution S6Shortcuts S6 Challenge`: exit 0.
- Temporary `E1_Unit1Check.lean`, importing only `Cancellation` and checking
  the two fully qualified structure names: `lake env lean E1_Unit1Check.lean`, exit 0.
- Temporary `E1_Unit1Axioms.lean`, importing `Hopf.SphereTopology` and printing
  the three fully qualified lane-probe axioms: `lake env lean E1_Unit1Axioms.lean`,
  exit 0; each gives exactly `[propext, Classical.choice, Quot.sound]`.
- `git diff --check`: clean.

The probes are still in `Hopf/`; these axiom checks protect the consumers,
not a claim that the lane is complete.

## Probe-closure baseline — current E1 extraction boundary

GLM seat run by Devin/Astra, source revision
`ca8ed1f50ce29f702380211a0674e6e38da1c6e8`. The owner replaced the historical
197-declaration cut with the current Hopf dependency closure of the three
lane probes and requested one batch move followed by review and cleanup.
The already-landed cubic core and the three historical drafts were untouched.

| Target under `Lib/Geometry/Manifold/Morse/` | Moved declarations | Headline |
|---|---:|---|
| `ConnectionCancellation.lean` | 298 | `MorseCancellation.cancel_of_transverse_level_isotopy` |
| `RearrangementTheorem.lean` | 44 | `MorseRearrangement.exists_morse_rearrangement_of_no_connection` |
| `Birth.lean` | 26 | `MorseCancellation.exists_excellent_indexed_morse_birth` |

Source removals: `Hopf/DifferentialTopology.lean` 5,
`Hopf/SingularHomology.lean` 293, `Hopf/SphereTopology.lean` 70; **368 total**.
The preceding `ca8ed1f` unit moved two data structures, making 370 declarations
for this restart. Stock census: **2,661 → 2,293** for this batch; **2,663 →
2,293** including the preceding data-structure unit. The prefix list is unchanged.

### Provenance and import boundary

Every moved declaration, including its local attribute wrapper, is byte-identical
to the source. No proof term, theorem statement, identifier or namespace changed.
Exact source ranges, SHA-256 hashes and destinations for all 368 declarations
are committed in `Lib/reports/E1-probe-closure-provenance.json` (SHA-256
`feea0ac41f40a18fb4625a46a1d26959fb7f124179d36f73ab55ef53f04d46c1`).
The source remainder differs only by the removals, explicit new module imports
and blank-line cleanup at the vacated ranges.

`ConnectionCancellation` imports the existing `Rearrangement` toolbox, which
already reaches `Cancellation` and `Transversality.Basic`; the reverse import
cycle is avoided. The two theorem files import `ConnectionCancellation`.
The source `Hopf/` modules import the new modules explicitly; `Lib.lean`
registers all three. The five DifferentialTopology declarations retain their
local `NativeEuclideanEmbedding.tangentSpaceT2` instance in an isolated section;
all source opens, universes and notations are carried. Further subdivision of
this dependency cluster is a later in-Lib layout change, not a fresh extraction.

### Verification

Pinned Lean 4.33.0; `ps` inspected before each build.

- `lake build Lib.Geometry.Manifold.Morse.ConnectionCancellation Lib.Geometry.Manifold.Morse.RearrangementTheorem Lib.Geometry.Manifold.Morse.Birth`: exit 0.
- `lake build Hopf.SphereTopology`: exit 0.
- `lake build Lib Solution S6Shortcuts S6 Challenge`: exit 0.
- `lake env lean Lib/AxiomAudit.lean`: exit 0.
- Temporary `E1_AllProbeCheck.lean` imported only the three new Lib modules;
  `lake env lean E1_AllProbeCheck.lean` returned exit 0. Each of the three
  headline theorems has exactly `[propext, Classical.choice, Quot.sound]`.
  Their permanent probes are in `Lib/AxiomAudit.lean`.
- Source conservation and target byte-identity checks passed for all 368
  declarations, with no exceptions; source order and provenance metadata match.
- `python3 scripts/lib_stock_census.py --update`: baseline lowered to 2,293;
  `git diff --check`: clean.

All three headline theorems are now declared in `Lib/`, not `Hopf/`. This meets
the owner's current E1 extraction boundary. The historical blanket blockers
above no longer describe this boundary. Per-declaration docstrings, independent
textbook review, finer layout and Mathlib API alignment are not claimed complete
by this pure-move baseline.

## Layout split scoping (2026-09-13, GLM seat, branch `lib/A-10-layout`)

The seat file's item 5 (split `Morse/ConnectionCancellation.lean`, 298
declarations, and `Morse/Cancellation.lean`, 303, along the Milnor h-cobordism
sections into `Cubic`, `CubicFlow`, `Connection`, `Cancellation`) was scoped
before moving anything. Family-level partition analysis: the four targets are
not family-separable — the dotted/bare reference edges across a
family-to-file assignment are bidirectional (CubicFlow -> Cancellation 48,
Cancellation -> CubicFlow 25, Connection <-> Cancellation 12/12), i.e. the
family cut is cyclic and cannot be an import DAG. A section-contiguous cut
shows the same crossings (the cubic-model sections call the suspended-flow
chart machinery and vice versa).

The split therefore requires a per-declaration placement optimised for an
acyclic four-module import order (mover machinery from the Wang landing
applies: full-block reference harvest, topological assembly, error-driven
placement fix-up). Not attempted in this seat pass; recorded as the seam with
the edge counts above so the next pass starts from data, not guesswork.

## Layout split done (2026-09-14)

`NEXT_STEPS.md` item 9. `Morse/ConnectionCancellation.lean` (298 declarations,
9,860 lines) and `Morse/Cancellation.lean` (303, 6,359 lines) are replaced by
four modules under `Lib/Geometry/Manifold/Morse/`, imported in the order
`Cubic -> CubicFlow -> (Rearrangement) -> Connection -> Cancellation`. Every
declaration keeps its name, namespace, statement and proof (blocks moved
verbatim); only the module docstrings are new.

Procedure: every top-level block (attribute-`in` lines, docstring, declaration,
proof) of the two files was harvested with the set of the other 600
declarations it references (full dotted name, bare last component, and
dotted suffixes; edges restricted to declarations earlier in the old file
order, so a false positive only constrains the order). Each declaration got
the module of its `/-! ### ... -/` section (cubic model / flow and suspension /
connection control / cancellation proper). Two hard constraints: the module
importers `Morse/Rearrangement`, `Transversality/Basic` and
`Immersion/Relative` use 50 old-`Cancellation.lean` declarations (28 full,
22 bare), which pins them (and their closure) to `Cubic`/`CubicFlow`, the
`module` files before `Rearrangement`; and the old
`ConnectionCancellation.lean` declarations reference 23 declarations of
`Rearrangement`/`Transversality`/`Relative` (full names), so they can only
live in `Connection`/`Cancellation`, the non-module files after
`Rearrangement` (this also keeps the `module` / non-module compilation mode
of every declaration unchanged). The repair then moved a declaration to the
earliest module that needs it, iterated to a fixed point: 25 declarations
moved, 0 backward edges.

| module | declarations | from old `Cancellation.lean` | from old `ConnectionCancellation.lean` | lines | header |
|---|---|---|---|---|---|
| `Cubic` | 143 | 143 | 0 | 2706 | `module`, public imports of the old `Cancellation.lean` |
| `CubicFlow` | 91 | 91 | 0 | 1623 | `module`, + `public import Morse.Cubic` |
| `Connection` | 231 | 41 | 190 | 8180 | non-module, `import Morse.Rearrangement` |
| `Cancellation` | 136 | 28 | 108 | 3805 | non-module, `import Morse.Connection` |
| total | 601 | 303 | 298 | 16314 | |

Reference edges after placement (needed -> needing): Cubic->Cubic 342,
Cubic->CubicFlow 39, Cubic->Connection 139, Cubic->Cancellation 90,
CubicFlow->CubicFlow 146, CubicFlow->Connection 237,
CubicFlow->Cancellation 84, Connection->Connection 312,
Connection->Cancellation 33, Cancellation->Cancellation 215; backward 0.
Sections split by the repair: strict descent and level basins (5 to `Cubic`),
signed level time (1), height-translating flows (2), the cancelled descent
field (3 to `Connection`, 1 to `CubicFlow`), basin-preserving endpoint clocks
(2 to `CubicFlow`), nowhere-dense critical pieces (3/1), quadratic germs (5 to
`CubicFlow`), native Morse index (2/1), omega limits (5 to `CubicFlow`),
minimum basins (1), native cancellation data (1 to `Connection`); all
`ConnectionCancellation` sections stayed whole.

Importers: `Rearrangement`, `Transversality/Basic`, `Immersion/Relative` and
`CellStructure` now import `Morse.CubicFlow`; `Birth` and
`RearrangementTheorem` import `Morse.Cancellation`; the 26 `Hopf/` importers
replace `Morse.ConnectionCancellation` by `Morse.Connection` (their
`Morse.Cancellation` line stays); `Lib.lean` lists the four modules.

Verification: the multiset of the 601 declaration blocks (comment-stripped,
whitespace-normalised) of the four new files equals that of the two old
files at `304a0fea` (601 = 601, all distinct, no missing, no extra).
`lake build Lib`: `Build completed successfully (8819 jobs)`;
`python3 scripts/lib_stock_census.py --check`: `ratchet PASS: 1586 <= baseline 1648`
(the census counts `Hopf/` declarations only, untouched by the split).
The full chain `lake build Solution S6Shortcuts S6 Challenge` (8858 jobs) was
still running at 8847/8858 with no error when this section was written; see
the split commit message for the final result.
